const AdService = {
    async addNewAd(title, year, generation, mileage, owners, modification, engineCapacity, gearbox, drive, equipment, body, color, wheel, vin, user) {
        
    }
}

module.exports = AdService